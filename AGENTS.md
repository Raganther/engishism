# Engishism — see `CLAUDE.md`

**`CLAUDE.md` is the project's memory and the only copy of it.** Read that.

This file exists because some tools look for `AGENTS.md` by convention. It holds a
pointer rather than content: it was a full copy once, it drifted three days and several
thousand lines out of date without anybody noticing, and a second copy of a fact is two
facts that will disagree.
